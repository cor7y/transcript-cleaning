---
name: transcript-cleaning
description: >
  Clean meeting/interview transcripts produced by speech-to-text — remove filler, fix homophone errors, merge fragmented same-speaker turns. Use when the user has a transcript and wants cleaning.
---

# 转录文稿语言清洗

核心目标是消除语气词、修正同音错字、补全断句；同发言人连续段合并。最终提供一份人类可读、可复核的清洗稿。

## 产出物与行为模式

无论输入什么文件格式（如 `xxx.docx`），默认在同级目录生成清洗后的文件 `xxx_cleaned.md`。

## 专有名词与词表沉淀 (CLAUDE.md)

跨文档反复出现的专有名词或高频错字，在与用户确认后，记录到同级目录的 `CLAUDE.md` 。
- **若 `CLAUDE.md` 已存在，追加到文件尾。**
- 若不存在，则创建并写入。
写入格式示例：
```markdown
| 错误识别 | 正确写法 | 备注/上下文 |
|----------|----------|-------------|
| 奔塔     | 灯塔     | 灯塔工厂    |
```

## 清洗规则

### 1. 三档确定性处理
按修正确定性分三档，标记明确，方便复核。
- **100%确定**（明显谐音、`CLAUDE.md`中已确认的词汇、明显语气词如“嗯”“啊”）：直接替换或删除，不标注。
- **高概率猜测**（有改法但不绝对）：保留原词，在其后加 `*[更正后词]`。例：`电机*[电极]`。
- **完全猜不到**：保留原词，在其后加 `*`。例：`海光之战*`。

### 2. 发言人编号
- 编号仅在当前文档内区分发言人。
- 严格保留 ASR 原编号，**不重排、不合并、不新增发言人**。

### 3. 正文排版
- 发言人头（如 `发言人 1`）与下方内容之间**不留空行**。
- 不同发言人之间**空一行**。
- 连续同一发言人的多段（中间没被打断）合并为一段，换行但不留空行。被其他发言人打断后的不合并。
- 半截话用破折号 `——` 结尾，保留发言人头。
- 标点一律使用半角逗号 `,` 和半角句号 `。`。

## 输出格式要求

清洗后的文档头部需完全保留原标题、时长等元信息，并必须在开头追加以下清洗说明：

```markdown
> 清洗说明：消除语气词、修正同音错字、补全断句；同发言人连续段合并；格式整理。
> 100% 确定已直接修改；高概率猜测用 `原词*[更正后词]` 标注；完全不确定的用 `原词*` 标注。
```

## 用户对话纪律

遇到以下情况，**主动询问用户**：
1. **高频未知专有名词或错字**：询问正确写法，确认后追加到 `CLAUDE.md`，随后在文中直接替换。
2. 批量积攒了多处未知的 `*` 标记，统一询问。

## 质量自查清单

提交清洗稿前自查：
- [ ] 没有为了通顺而猜测意图。
- [ ] 没有虚构发言人编号或合并不同发言人。
- [ ] 100% 确定的词汇已直接替换，没有残留 `[]` 或 `*`。
- [ ] 已在 cleaned 版头部写入清洗说明，原头部元信息未丢失。
- [ ] 确认的专有名词，已追加入 `CLAUDE.md` 。
