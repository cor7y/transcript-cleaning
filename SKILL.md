---
name: transcript-cleaning
description: >
  Clean meeting/interview transcripts produced by speech-to-text — remove filler, fix homophone errors, merge fragmented same-speaker turns. Use when the user has a transcript and wants cleaning.
---

# 调研会议转录文稿语言清洗

ASR(语音转文字)出来的稿子有三类问题:语气词填充、同音错字、半截话。用户拿到这种稿子最痛苦的不是"哪里错了",而是"哪里改了我没法验证"。所以这个 skill 的核心不是"清洗",是**让用户能在十分钟内复核完一整个文档**——通过把"我改了什么、为什么、有多确定"显式编码进文本本身。

---

## 工作流总览

```
原文/        ← ASR 直出,只读
   ↓ 清洗(本 skill)
清洗后/      ← 人类可读、可复核
   ↓ (可选)
洞察提取/    ← 提取摘要、用户画像、行为原型、洞察(交给 transcript-to-insights skill)
```

这个 skill 只处理 `0_原文/ → 1_清洗后/` 这一步。其他阶段不要在这里做,会污染上下文判断。

---

## 项目骨架(首次启动一个清洗项目时建立)

如果用户给的是单独一两份稿子,直接清洗即可,不必建骨架。
**当用户描述的是"一批"会议记录(多个板块/多个文档/需要分阶段处理)时,先建立下面这套骨架,再开始清洗。**

### 目录结构

```
<project-root>/
├── CLAUDE.md          ← 项目级清洗约定 + 专有名词确认表
├── PROGRESS.md        ← 跨会话恢复用,每完成一个文档就标记
├── 0_原文/            ← ASR 直出文档,只读
├── 1_清洗后/          ← 清洗后产物,文件名与原文一致
└── 2_洞察提取/        ← 后续洞察提取产物
```

文件命名按板块编号(`1.1 开场.md`、`1.2 业务沟通预备.md`、`2.1 产品检测.md`……),便于按板块批处理和上下文隔离。

### PROGRESS.md 模板

```markdown
# <项目名> 处理进度

> 跨对话恢复用:读此文件即可恢复当前位置,无需回溯历史。
> 更新规则:每完成一个文档的清洗,立即标记。

---

## 总览

| 阶段 | 进度 | 状态 |
|------|------|------|
| 阶段一:语言清洗 | 0 / N | 未开始 |

---

## 板块 X

| 文档 | 清洗 | 备注 |
|------|:----:|:----:|
| X.1 ....md | pending | |
| X.2 ....md | pending | |

---

## 待确认项(清洗阶段积累)

- [ ] `海光之战*` — 推断不出含义,待与用户核对
- [ ] `Essence*` — 可能是 Accenture/EY,语境存疑

---

## 执行记录

| 时间 | 动作 | 结果 |
|------|------|------|
| YYYY-MM-DD | 创建本进度文件 | 初始化 |
```

### CLAUDE.md 模板要点

项目级 CLAUDE.md 至少包含:
1. 三档确定性处理表(从本 skill 拷下来,可裁剪)
2. **专有名词确认表**(初始为空,清洗过程中逐项与用户确认后写入)
3. 高频错字表(同上)
4. 输出格式约定(本 skill 的"输出格式"小节)
5. 上下文隔离规则

---

## 清洗规则

### 红线:宁缺毋错

清洗的反义词不是"不动",而是"乱改"。下面这些行为比留下错字更糟,因为它们会**让用户失去对稿子的信任**:

- 听不清就猜:不确定的句子或语义模糊的片段,**保留原识别词 + `*`**,不要按"常理"补
- 脑补意图:不要根据"他应该是想说……"补全,他说什么是什么
- 虚构发言人:不要新增、不要合并、不要重新编号
- 跨文档抄答案:这一份文档里的"上下文"只能来自这一份文档(详见"上下文隔离")

### 三档确定性处理

按修正确定性分三档,让用户复核时只需把注意力放在标了 `[]` 或 `*` 的地方。

| 确定性 | 处理方式 | 示例 |
|--------|----------|------|
| 100% 确定(明显谐音、用户已确认的专有名词、明显语气词) | 直接替换/删除,不标注 | `端塔→灯塔`、`阿麦顺→安脉盛`、删除"呃""那个" |
| 有改法但不绝对(高概率猜测) | **保留原词**,在其后加 `[更正后词]` | `电机[电极]`、`演技[演进]` |
| 完全猜不到/听不清/半句话 | **保留原词**,在其后加 `*` | `海光之战*`、`艺术*` |

**两种标注的语义:**
- `[]` 说"我提了一个修正建议",`*` 说"这里有问题但我没法改"
- 用户复核时:看到 `[]` 就核对修正是否合理;看到 `*` 就考虑要不要听录音、查上下文或放弃
- **`[]` 和 `*` 不叠加**——能提修正就用 `[]`,提不出就用 `*`
- 原词永远不动,所有"添加"放在原词右侧。这样删掉所有 `[...]` 和 `*` 就能回到 ASR 原稿,可逆。

### 100% 确定的判定标准

"100% 确定"是个高门槛,以下三类才算:
1. **明显谐音的常用词**:语境锁死且只有一种合理写法(如"端塔工厂"→"灯塔工厂")
2. **用户已确认并写入项目 CLAUDE.md 的专有名词**(如客户公司名、产品名)
3. **明显的语气词填充**:"嗯""啊""呃""那个那个"等不承载语义的填充词

只要语境允许第二种合理解读,就降级为"高概率猜测"档,用 `[]` 标注。

### 专有名词锁定表(项目 CLAUDE.md 维护)

跨文档反复出现的专有名词,**第一次遇到时与用户确认,确认后写入项目 CLAUDE.md**,后续所有文档直接替换不再标注。这样避免同一个错字在 20 份文档里以 20 种方式被清洗。

表格结构(项目 CLAUDE.md 中):
```markdown
**专有名词(用户已确认)**:
| 错误识别 | 正确名称 | 备注 |
|----------|----------|------|
| 阿麦顺 / 门生 / 阿麦社 | 安脉盛 | 发起方咨询公司名 |
| 弹性模具 | 海信模具 | 受访方公司名 |

**行业/概念高频错字**:
| 错误识别 | 正确写法 | 上下文 |
|----------|----------|--------|
| 端塔 / 奔塔 | 灯塔 | 灯塔工厂 |
| 用力 | 用例 | use case |
```

**易错提醒:** 已确认的词条直接替换为正确写法,**不要加方括号、不要加 `*`**。
错例:`阿麦社[安脉盛]`  正确:`安脉盛`

清洗完毕通读一遍,把已确认词条上残留的 `[]` 和 `*` 全部去掉,再保存。

### 发言人编号规则

- 编号**仅在当前文档内区分发言人**
- **跨文档不对应**:`1.1` 文档的"发言人1"和 `2.1` 文档的"发言人1"不是同一个人
- 处理时只关注当前文档内的编号体系,不要拿其他文档的角色信息来"修正"本文档的编号
- 编号一律保留 ASR 原编号,**不重排、不合并、不新增**

---

## 输出格式

清洗后文档保存到 `1_清洗后/`,文件名与原文一致。

### 文档头

完全保留原文档的 标题 / 创建时间 / 转写时长 / 关键词 等元信息,只清洗"## 转文字"小节。在文档头追加一行清洗说明:

```markdown
> 清洗说明: 消除语气词、修正同音错字、补全断句;同发言人连续段合并;
> 100% 确定的替换(见 CLAUDE.md 确认表)直接替换;高概率猜测用 `原词[更正后词]` 标注,
> 完全不确定的用 `原词*` 标注。
```

### 正文排版规则

```
发言人 1
内容直接接在下一行,中间不留空行。

发言人 2
不同发言人之间空一行。

发言人 1
连续同一发言人的多段(中间没被打断)合并为一段。
```

- 发言人头(`发言人 X`)与下方语句**不留空行**
- 不同发言人之间空一行
- 连续同一发言人的多段合并;**被其他发言人打断后的不合并**
- 半截话用破折号 `——` 结尾,保留发言人头(说明被打断)
- 标点全部使用半角逗号 `,` 和半角句号 `。`(保持转写器原风格,不替换为全角)

### 标注样式速查

| 场景 | 写法 | 复核动作 |
|------|------|----------|
| 同音错字、确认的专有名词、语气词 | 直接改,不标注 | 不用看 |
| 高概率猜测(敢提修正) | `电机[电极]` | 核对修正是否合理 |
| 听不清(提不出修正) | `海光之战*` | 听录音或确认放弃 |
| 半截话(被打断) | `他这个是——` | 不补全 |
| 漏字补全(语境可明确) | `[然后]`(直接补)或 `[然后?]`(留疑) | 看是否符合原意 |

---

## 上下文隔离

### 为什么

把不同板块的内容塞在一个 agent 的上下文里,会发生这两类污染:
- 清洗时拿 A 板块的术语去"修正" B 板块的真实表达
- 复述时把 A 板块说的话错记成 B 板块说的(后续洞察提取阶段尤其致命)

### 怎么做

- **单 agent 一次只处理一个板块的所有文档**(如只处理 1.x 系列)
- **新 agent 启动时不携带其他板块的稿子或产物**;只读项目 CLAUDE.md(里面只有约定和确认表,没有具体板块内容)
- **输出隔离**:每个板块独立输出到 `1_清洗后/` 下的对应文件,不在一个文件里混编
- **并行上限 3**:同时运行的清洗 agent 不超过 3 个,避免相互干扰和 token 爆炸

---

## PROGRESS.md 跨会话恢复

每完成一个文档的清洗,**立即**更新 PROGRESS.md:

1. 在板块表里把对应行的状态从 `pending` 改为 `done`
2. 总览表里的"进度"数字 +1
3. 在"执行记录"追加一行:`YYYY-MM-DD | 清洗 X.Y .md | 完成`
4. 如果清洗中遇到新的待确认项(高频出现的 `*` 标注词),追加到"待确认项"区,标上来源

下次会话或下一个 agent 进来时,**只读 PROGRESS.md 就能知道做到哪了**,不需要回溯任何对话历史。

---

## 与用户的对话纪律

清洗过程中遇到下面这些情况,**停下来问用户**,不要自己拍板:

1. **发现一个高频专有名词**(在多份稿子里反复出现的可疑词)。例:"`阿麦顺`、`门生`、`阿麦社` 这几种识别我推测都是同一个名字,可能是发起方公司名。要不要确认一下正确写法?确认后我加到项目 CLAUDE.md 的确认表里,后续直接替换。"
2. **发现一个高频错字**(模式化的同音错字)。同样的处理流程。
3. **某个 `*` 词条在很多文档里都出现但都猜不到含义**,标到 PROGRESS.md 的"待确认项"里,等批量过一遍时再问。

不要每遇到一个 `*` 就问一次——那样太碎。攒到一批再问,或者用户主动复核时一并解决。

---

## 质量底线(自查清单)

清洗一份文档后,提交前自查:

- [ ] 没有为了"看起来通顺"猜测说话人意图
- [ ] 没有虚构发言人编号、没有合并或新增发言人
- [ ] 100% 确定的替换没有残留 `[]` 或 `*`
- [ ] 高概率猜测用 `原词[更正后词]`、完全猜不到用 `原词*`,**不叠加**
- [ ] 没有引用其他板块/文档的内容来"修正"本文档
- [ ] 文档头元信息(标题、时长、关键词)完整保留
- [ ] PROGRESS.md 已更新

---

## 与下一阶段(洞察提取)的衔接

清洗后的稿子要交给 `transcript-to-insights` skill 做洞察提取时,告诉对方两件事:

1. **每份文档的发言人编号互相独立**——洞察提取时引用发言人,要带文档号(如"发言人 3 @1.4"而非孤立的"发言人 3")
2. **带 `*` 的字段是未确认信息**——洞察提取不要把 `*` 词条当作可靠引文使用,如果一个洞察依赖某个 `*` 词条,需先回到本 skill 解决这个 `*`
3. **带 `[]` 的字段是高概率猜测**——可以引用,但引用时建议把 `原词[更正后词]` 一起带上,让下游读者知道这是 ASR 的原文 + 清洗时的修正建议

清洗稿件的引文位置标记建议用 `@<文档号> L<行号>`(例:`@1.4 L407`),方便洞察提取阶段反向追溯到原文。
