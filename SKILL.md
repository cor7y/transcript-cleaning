---
name: transcript-cleaning
description: >
  Clean transcripts produced by speech-to-text — remove filler, fix homophone errors, merge fragmented same-speaker turns, repair speaker attribution.
---

# 转录文稿清洗

核心目标是消除语气词、修正同音错字、补全断句、还原真实发言人；同发言人连续段合并。最终提供一份可复核的清洗稿。

## 开工前先确认

1. **若上传了多个转录版本** → 先读 `references/dual-source-alignment.md`。
2. **若项目存在术语表（人名/系统名清单）** → 先读。

## 清洗规则

### 1. 三档确定性处理

按修正确定性分三档，标记明确，方便复核。
- **100% 确定**（明显谐音、用户已确认的词、**命中项目术语表已有变体的词**、明显语气词如"嗯""啊"）：直接替换或删除，不标注，不必逐条问用户。
- **高概率猜测**（有改法但不绝对）：`ASR原词*[更正后词]`。例：`电机*[电极]`。
- **完全猜不到**：`ASR原词*`。例：`海光*`。

### 2. 发言人

**标签**
- 已知真名时用**姓氏单字**（周、陈、冯……）；同姓或易混的人保留全名（如 `旭峰`）。
- 匿名编号（`发言人 1`）且用户没给真名时保持原样。
- 不凭空新增发言人。例外只有一种：从一个 ASR 标签里确实能拆出第二个人（对话回指、身份内容可证）——拆之前先问用户，拆完在头部写明。

**断句归属按语义判断复核**
- 单麦克风转录常把一句话劈到两个发言人下 → 按语义识别判断，**合并到真实发言人**，不要保留 ASR 原归属再加破折号。
- 反过来，一个 ASR 段落里同时含提问和回答（从"你/您"称呼切到"我/我们"自述）→ **拆成多轮**。
- "不凭空造发言人"不等于"保留明显错误的切分"。

**不要轻易改判**
- **发言量少不是错标的证据**。只有确有对话回指矛盾（被点名的人下一轮没应答、内容与该人身份冲突）才动，**移除发言人标签前先问用户**。
- 孤立标签的两种形态：
  - **ASR 切分 artifact**：孤立标签＋全部不成句＋每条都能接上相邻真实发言人的上一句 → 按语义并回。
  - **真实但发言少的人**：用户手标的、且每段都成句 → 不动。

**共用会议室设备 → 一个标签装一屋子人** 
飞书把会议室里所有未匹配声纹的人并进设备主人的标签。看到某个标签发言量异常大、话题跨度异常宽，先怀疑这个。

**「X 用 Y 的设备」里的 X 也可能是错的人**
判据：**先问在场名单里谁没有标签，再看谁被点名后下一轮谁应答。**

### 3. 删除范围

- 会议外串音可以整段删，**不要自己猜切点**：先找证据（话题突变处；有双源时见 reference 里的方法），把候选边界给用户确认。同样是"外来声音"，用户可能要"只删外人进屋的会议声、保留寒暄和私聊"，而不是整段砍掉。

## 输出规范

无论输入什么文件格式（如 `xxx.docx`），默认在同级目录生成清洗后的文件 `xxx_cleaned.md`。

### 1. 正文排版

- 发言人头（如 `发言人 1`）与下方内容之间**不留空行**。
- 不同发言人之间**空一行**。
- 连续同一发言人的多段（中间没被打断）合并为一段。被其他发言人打断后的不合并。
- 真实被打断的半截话用破折号 `——` 结尾，保留发言人头；ASR 切分造成的半截按上面"断句归属"合并。
- 中文文段使用全角标点「，」「。」。

```
旭峰
内容内容内容

冯
内容内容内容
```

### 2. 头部格式

完全保留原标题、时长等元信息，之后**只追加两行清洗说明**：
```markdown
> 清洗说明：消除语气词、修正同音错字、补全断句；同发言人连续段合并；格式整理。
> 100% 确定已直接修改；高概率猜测用 `原词*[更正后词]` 标注；完全不确定的用 `原词*` 标注。
```

**唯一例外**：发言人归属做过非平凡还原（拆出第二个人、合并同一人的两个标签、双源重标）或删过整段时，在两行说明**之上**加一行：
```markdown
> 发言人："发言人 3"为冯某换麦后的第二标签，已并入冯；从"周"标签中拆出电话接入的客户方，记为"客户"；删除会议外串音（约 38:10 起）。
```

人名/系统名统一说明**不写进头部**——写进项目人员列表/术语表，或在对话里口头汇报。

## 用户交互纪律

遇到以下情况，**主动询问用户**：
1. 需要更多上下文才能下的判断和总结：提问，不要自行发散或脑补。
2. 高频未知专有名词或错字：询问正确写法，在文中直接替换（并建议补进项目术语表）。
3. 批量积攒了多处 `*` 标记：统一询问一次。
4. 要改判发言人、拆出新发言人、删整段：先问。

## 交付汇报

- 一两句说明产出位置和做了哪些非平凡处理（改判、拆分、删除、人名统一）。
- **关键单句提醒**：若稿中出现影响结论的单句，提醒用户回听录音。一个"不"字漏听或多听就能让结论反转。

## 自查清单

提交清洗稿前自查：
- [ ] 跨标签断句已按语义合并，问答混段已拆开。
- [ ] 100% 确定的词已直接替换；标注方向全部是 `原词*[更正]`，没有写反。
- [ ] 双源场景：已按 reference 做完**反向覆盖检查**。

[最近更新：2026-09-11 吴昊]